use loadFile::*;

pub struct Tank {
    size: (usize, usize),
    frame: usize,
    tank_chars: Vec<Vec<String>>,
}

impl Tank {
    pub fn new(name: String) -> Self {
        return Self {
            size: (0, 0),
            frame: 0,
            tank_chars: 
        }
    }
}
